{
  "name": "guitar-studio",
  "version": "1.17.0",
  "private": true,
  "description": "Guitar theory & practice app. Authored as editable sources under src/ and assembled by build.js into a single zero-runtime-dependency index.html (the deployable artifact).",
  "scripts": {
    "build": "node build.js",
    "test": "npm --prefix tests test",
    "pretest": "node build.js"
  }
}
